flask>=3.0
pymupdf>=1.24
requests>=2.31
rapidfuzz>=3.6
beautifulsoup4>=4.12
lxml>=5.0
playwright>=1.44
# Enables model-grade relevance judging instead of the lexical fallback. Without
# it installed, OPENAI_API_KEY is ignored and every run falls back to lexical.
openai>=1.40
